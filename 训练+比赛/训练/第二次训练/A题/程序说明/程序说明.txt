gyhbit_dmap1.m-------------分组法求点方向图
    若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。

gyhbit_dmap1.m-------------邻域方向模板法求点方向图（也称B.M.Mehtr法）
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。


gyhbit_dmap3.m-------------切缝法求点方向图
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。


gyhbit_dmap4.m-------------改进方法求点方向图
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。

gyhf_cd_dmap4.m-------------由改进方法得到的点方向图求连续分布方向图
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。

gyhf_cd_dmap488.m-------------根据连续分布方向图求块方向图 利用8*8的窗口
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。

gyhpjfxhsa.m-------------由指纹点方向图求解指纹的块方向图
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。

rao.m-------------利用梯度法求解指纹的块方向图
若要求其他的指纹图像的点方向图,将“fp=imread('finger.tif');  %读入指纹图象”里的imread的参数修改成所需要的图像文件名。图像准备好后，在matlab中直接运行即可。
